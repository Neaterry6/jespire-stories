'use client'
import { FormEvent, useState } from 'react'
import { signInGoogle, signInEmail, signUpEmail } from '@/hooks/useAuth'
import { useRouter } from 'next/navigation'
import { toast } from 'sonner'
export default function Auth(){const [signup,setSignup]=useState(false);const [name,setName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [busy,setBusy]=useState(false);const router=useRouter();async function submit(e:FormEvent){e.preventDefault();setBusy(true);try{if(signup)await signUpEmail(name,email,password);else await signInEmail(email,password);router.push('/feed')}catch(e:any){toast.error(e.message?.replace('Firebase: Error (auth/','').replace(').','')||'Authentication failed')}finally{setBusy(false)}}async function google(){try{await signInGoogle();router.push('/feed')}catch(e:any){toast.error(e.message||'Google sign-in failed')}}return <div className="flex min-h-[calc(100vh-64px)] items-center justify-center px-5 py-16"><div className="paper w-full max-w-md rounded-[2rem] p-8 shadow-soft sm:p-10"><p className="text-xs uppercase tracking-[.25em] text-black/40">Welcome to Jespire</p><h1 className="mt-3 font-serif text-4xl">{signup?'Create your reading room.':'Come back to the page.'}</h1><form onSubmit={submit} className="mt-8 space-y-4">{signup&&<input required value={name} onChange={e=>setName(e.target.value)} placeholder="Your name" className="w-full rounded-2xl border border-black/10 bg-white px-4 py-3 outline-none focus:border-black"/>}<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address" className="w-full rounded-2xl border border-black/10 bg-white px-4 py-3 outline-none focus:border-black"/><input required minLength={6} type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" className="w-full rounded-2xl border border-black/10 bg-white px-4 py-3 outline-none focus:border-black"/><button disabled={busy} className="w-full rounded-2xl bg-[#171313] py-3.5 text-white disabled:opacity-50">{busy?'Please wait…':signup?'Create account':'Sign in'}</button></form><div className="my-5 flex items-center gap-3 text-xs text-black/30"><span className="h-px flex-1 bg-black/10"/>OR<span className="h-px flex-1 bg-black/10"/></div><button onClick={google} className="w-full rounded-2xl border border-black/10 bg-white py-3.5 font-medium">Continue with Google</button><button onClick={()=>setSignup(!signup)} className="mt-6 w-full text-sm text-black/55">{signup?'Already have an account? Sign in':'New here? Create an account'}</button></div></div>}
