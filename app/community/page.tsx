'use client'
import { ChangeEvent, useEffect, useRef, useState } from 'react'
import { addDoc, collection, deleteDoc, doc, onSnapshot, orderBy, query, serverTimestamp, updateDoc } from 'firebase/firestore'
import { getDownloadURL, ref as storageRef, uploadBytes } from 'firebase/storage'
import { db, storage } from '@/lib/firebase'
import { useAuth } from '@/hooks/useAuth'
import { getJespireRole } from '@/lib/roles'
import { ArrowLeft, Camera, FileText, Flame, Heart, Laugh, Mic, Paperclip, Pin, Play, Reply, Send, Smile, Trash2, Users, X } from 'lucide-react'
import { toast } from 'sonner'
type Message={id:string;userId:string;displayName?:string;photoURL?:string;text?:string;replyToText?:string|null;mediaUrl?:string|null;mediaType?:string|null;mediaName?:string|null;createdAt?:{seconds?:number}|null;pinned?:boolean;reactions?:Record<string,string>}
const reactions=[['heart','Heart',Heart],['laugh','Laugh',Laugh],['fire','Fire',Flame]] as const
export default function Community(){const{user}=useAuth();const[messages,setMessages]=useState<Message[]>([]);const[text,setText]=useState('');const[reply,setReply]=useState<Message|null>(null);const[busy,setBusy]=useState(false);const[recording,setRecording]=useState(false);const[input,setInput]=useState<HTMLInputElement|null>(null);const recorderRef=useRef<MediaRecorder|null>(null);const streamRef=useRef<MediaStream|null>(null);const chunksRef=useRef<Blob[]>([]);const role=getJespireRole(user?.email);const staff=role!=='user';useEffect(()=>{const q=query(collection(db,'messages'),orderBy('createdAt','asc'));return onSnapshot(q,s=>setMessages(s.docs.map(d=>({id:d.id,...d.data()} as Message)).slice(-200)),e=>toast.error(e?.message||'Could not load community.'))},[]);useEffect(()=>()=>streamRef.current?.getTracks().forEach(t=>t.stop()),[])
 async function uploadMedia(file:File,kind:'image'|'file'){if(!user)return toast.info('Sign in to join the community.');if(file.size>10*1024*1024)return toast.error('Keep attachments under 10 MB.');setBusy(true);try{const safe=file.name.replace(/[^a-zA-Z0-9._-]/g,'_');const snap=await uploadBytes(storageRef(storage,`chat-media/${user.uid}/${Date.now()}-${safe}`),file,{contentType:file.type||'application/octet-stream'});const url=await getDownloadURL(snap.ref);await addDoc(collection(db,'messages'),{userId:user.uid,displayName:user.displayName||'Reader',photoURL:user.photoURL||'',text:'',mediaUrl:url,mediaType:kind==='image'?'image':file.type||'file',mediaName:file.name,replyToText:reply?.text||null,reactions:{},pinned:false,createdAt:serverTimestamp()});setReply(null);toast.success(kind==='image'?'Image sent.':'File sent.')}catch(e:any){toast.error(e?.message||'Upload failed.')}finally{setBusy(false)}}
 async function onFile(e:ChangeEvent<HTMLInputElement>){const f=e.target.files?.[0];e.target.value='';if(f)await uploadMedia(f,f.type.startsWith('image/')?'image':'file')}
 async function send(){if(!user)return toast.info('Sign in to join the community.');if(!text.trim())return;setBusy(true);try{await addDoc(collection(db,'messages'),{userId:user.uid,displayName:user.displayName||'Reader',photoURL:user.photoURL||'',text:text.trim(),replyToText:reply?.text||null,reactions:{},pinned:false,createdAt:serverTimestamp()});setText('');setReply(null)}catch(e:any){toast.error(e?.message||'Message failed.')}finally{setBusy(false)}}
 async function react(m:Message,type:string){if(!user)return;try{await updateDoc(doc(db,'messages',m.id),{[`reactions.${user.uid}`]:type})}catch(e:any){toast.error(e?.message||'Reaction failed.')}}
 async function pin(m:Message){if(staff)await updateDoc(doc(db,'messages',m.id),{pinned:!m.pinned})}
 async function remove(m:Message){if(user&&(m.userId===user.uid||staff))await deleteDoc(doc(db,'messages',m.id))}
 async function toggleRecording(){if(recording){recorderRef.current?.stop();return}if(!user)return toast.info('Sign in to send a voice note.');if(typeof window==='undefined'||!navigator.mediaDevices?.getUserMedia||typeof MediaRecorder==='undefined')return toast.error('Voice recording is not supported here.');try{const stream=await navigator.mediaDevices.getUserMedia({audio:true});streamRef.current=stream;chunksRef.current=[];const recorder=new MediaRecorder(stream);recorderRef.current=recorder;recorder.ondataavailable=e=>{if(e.data.size)chunksRef.current.push(e.data)};recorder.onstop=async()=>{setRecording(false);stream.getTracks().forEach(t=>t.stop());const blob=new Blob(chunksRef.current,{type:recorder.mimeType||'audio/webm'});if(!blob.size)return;setBusy(true);try{const snap=await uploadBytes(storageRef(storage,`chat-media/${user.uid}/${Date.now()}-voice.webm`),blob,{contentType:blob.type});const url=await getDownloadURL(snap.ref);await addDoc(collection(db,'messages'),{userId:user.uid,displayName:user.displayName||'Reader',photoURL:user.photoURL||'',text:'',mediaUrl:url,mediaType:'audio',mediaName:'Voice note',replyToText:reply?.text||null,reactions:{},pinned:false,createdAt:serverTimestamp()});setReply(null);toast.success('Voice note sent.')}catch(e:any){toast.error(e?.message||'Voice note failed.')}finally{setBusy(false)}};recorder.start();setRecording(true)}catch(e:any){toast.error(e?.message||'Microphone permission was not granted.')}}
 return <main className="mx-auto max-w-3xl px-0 pb-24 sm:px-3 sm:py-5"><header className="sticky top-16 z-20 flex items-center gap-3 border-b border-slate-200 bg-white/95 px-4 py-3 backdrop-blur dark:border-slate-800 dark:bg-[#070d19]/95 sm:rounded-2xl sm:border"><button onClick={()=>history.back()} className="grid h-10 w-10 place-items-center rounded-full bg-slate-100 dark:bg-slate-900" aria-label="Back"><ArrowLeft size={19}/></button><div className="grid h-10 w-10 place-items-center rounded-full bg-blue-600 text-white"><Users size={19}/></div><div className="min-w-0 flex-1"><h1 className="truncate font-semibold text-slate-950 dark:text-white">Jespire Community</h1><p className="text-xs text-slate-500">{messages.length?`${new Set(messages.map(m=>m.userId)).size} readers chatting`:'Start the room'}</p></div><div className="rounded-full bg-blue-50 px-3 py-2 text-xs font-medium text-blue-700 dark:bg-blue-950 dark:text-blue-200">{role==='user'?'Reader':role}</div></header><section className="overflow-hidden border-x border-slate-200 bg-[#eef3fa] dark:border-slate-800 dark:bg-[#05070b] sm:rounded-2xl sm:border"><div className="min-h-[68vh] overflow-y-auto px-3 py-5 sm:px-5">{messages.length?messages.map(m=>{const mine=m.userId===user?.uid;return <div key={m.id} className={`mb-4 flex ${mine?'justify-end':'justify-start'}`}><div className={`max-w-[86%] ${mine?'items-end':''}`}><div className={`flex gap-2 ${mine?'flex-row-reverse':''}`}><img src={m.photoURL||`https://api.dicebear.com/9.x/initials/svg?seed=${encodeURIComponent(m.displayName||'Reader')}`} className="mt-1 h-9 w-9 rounded-full object-cover" alt=""/><div className={`min-w-0 rounded-[1.35rem] px-3.5 py-2.5 shadow-sm ${mine?'rounded-tr-md bg-blue-600 text-white':'rounded-tl-md bg-white text-slate-800 dark:bg-[#171b22] dark:text-slate-100'}`}>{m.pinned&&<div className="mb-1 flex items-center gap-1 text-[10px] uppercase tracking-wider opacity-60"><Pin size={10}/> Pinned</div>}{!mine&&<p className="mb-1 text-xs font-semibold text-blue-600">{m.displayName||'Reader'}</p>}{m.replyToText&&<div className="mb-2 rounded-lg border-l-2 border-current/30 bg-black/5 px-2.5 py-1.5 text-xs opacity-75"><span className="font-semibold">Reply</span><br/>{m.replyToText}</div>}{m.mediaUrl&&m.mediaType==='image'&&<img src={m.mediaUrl} alt={m.mediaName||'Shared image'} className="mb-2 max-h-80 w-full rounded-xl object-cover"/>}{m.mediaUrl&&m.mediaType==='audio'&&<div className="mb-2 flex items-center gap-2 rounded-xl bg-black/5 p-2"><Play size={15}/><audio controls src={m.mediaUrl} className="h-9 max-w-[240px]"/></div>}{m.mediaUrl&&m.mediaType!=='image'&&m.mediaType!=='audio'&&<a href={m.mediaUrl} target="_blank" rel="noreferrer" className="mb-2 flex items-center gap-2 rounded-xl bg-black/5 p-2 text-sm underline"><FileText size={16}/><span className="truncate">{m.mediaName||'Attachment'}</span></a>}{m.text&&<p className="whitespace-pre-wrap break-words text-[15px] leading-5">{m.text}</p>}</div></div><div className={`mt-1 flex items-center gap-1 ${mine?'justify-end':'justify-start'} px-10`}><button onClick={()=>setReply(m)} className="rounded-full p-1.5 text-slate-400 hover:text-blue-600" aria-label="Reply"><Reply size={14}/></button>{reactions.map(([key,label,Icon])=><button key={key} onClick={()=>react(m,key)} className="rounded-full p-1.5 text-slate-400 hover:bg-blue-50 hover:text-blue-600" aria-label={label}><Icon size={14}/></button>)}{(m.userId===user?.uid||staff)&&<button onClick={()=>remove(m)} className="rounded-full p-1.5 text-slate-400 hover:text-red-500" aria-label="Delete"><Trash2 size={14}/></button>}{staff&&<button onClick={()=>pin(m)} className="rounded-full p-1.5 text-slate-400 hover:text-blue-600" aria-label="Pin"><Pin size={14}/></button>}</div></div></div>}) : <div className="flex min-h-[68vh] items-center justify-center text-center text-sm text-slate-400">No messages yet.<br/>Start the conversation.</div>}</div>{reply&&<div className="flex items-center gap-2 border-t border-slate-200 bg-white px-4 py-2.5 dark:border-slate-800 dark:bg-[#0d1117]"><Reply size={15} className="text-blue-600"/><span className="min-w-0 flex-1 truncate text-xs text-slate-500">Replying to: {reply.text||reply.mediaName||'attachment'}</span><button onClick={()=>setReply(null)} className="p-1 text-slate-400" aria-label="Cancel reply"><X size={16}/></button></div>}<div className="flex items-center gap-2 border-t border-slate-200 bg-white p-3 dark:border-slate-800 dark:bg-[#0d1117]"><input ref={setInput} type="file" hidden accept="image/*,.pdf,.txt,.doc,.docx" onChange={onFile}/><button onClick={()=>input?.click()} disabled={busy||recording} className="grid h-10 w-10 shrink-0 place-items-center rounded-full bg-slate-100 text-slate-500 hover:text-blue-600 dark:bg-slate-900" aria-label="Attach file"><Paperclip size={19}/></button><button onClick={()=>{if(input){input.accept='image/*';input.click()}}} disabled={busy||recording} className="grid h-10 w-10 shrink-0 place-items-center rounded-full bg-slate-100 text-slate-500 hover:text-blue-600 dark:bg-slate-900" aria-label="Send image"><Camera size={19}/></button><button onClick={()=>setText(v=>v+' ')} disabled={recording} className="grid h-10 w-10 shrink-0 place-items-center rounded-full bg-slate-100 text-slate-500 hover:text-blue-600 dark:bg-slate-900" aria-label="Emoji"><Smile size={19}/></button><input value={text} onChange={e=>setText(e.target.value)} onKeyDown={e=>e.key==='Enter'&&!e.shiftKey&&(e.preventDefault(),send())} placeholder="Message" className="min-w-0 flex-1 rounded-full border border-slate-200 bg-[#f7f9fc] px-4 py-2.5 text-sm text-slate-900 outline-none focus:border-blue-500 dark:border-slate-700 dark:bg-slate-900 dark:text-white"/><button onClick={text.trim()?send:toggleRecording} disabled={busy} className={`grid h-10 w-10 shrink-0 place-items-center rounded-full text-white ${recording?'bg-red-500':'bg-blue-600'}`} aria-label={text.trim()?'Send':'Voice note'}>{text.trim()?<Send size={17}/>:recording?<span className="h-2.5 w-2.5 animate-pulse rounded-full bg-white"/>:<Mic size={18}/>}</button></div></section></main>}
